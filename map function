#Write a Python program to triple all numbers in a given list of integers. Use Python map

a = range(1,11)

lam_fun = lambda x : x * 3

map_me = map(lam_fun, a)
print(list(map_me))

#Write a Python program to add three given lists using Python map and lambda.

l1 = range(1,6)
l2 = range(6,11)
l3 = range(11,16)

lam_fun = lambda x,y,z : x + y + z

map_me = map(lam_fun, l1,l2,l3)
print(list(map_me))

#Write a Python program to listify the list of given strings individually using Python map.

l1 = ['Red', 'Blue', 'Black', 'White', 'Pink']

lam_fun = lambda x : list(x)

map_me = map(lam_fun, l1)

print(list(map_me))

#Write a Python program to create a list containing the power of said number in bases raised to the corresponding number in the index using
#Python map.

power = range(1,11)
base = range(5, 16)

lam_fun = lambda y,x : y**x

map_me = map(lam_fun, base, power)
print(list(map_me))

#Write a Python program to square the elements of a list using the map() function.

l1 = range(1,11)

lam_fun = lambda x : x**2

map_me = list(map(lam_fun, l1))
print(map_me).
